spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop  # 테스트용 테이블 생성 후 삭제
    show-sql: true           # 테스트 환경에서 SQL 출력
